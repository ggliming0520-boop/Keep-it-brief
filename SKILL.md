---
name: keep-it-brief
description: 半自动加显式触发的防发散协作模式。用户明确说 "Keep it brief"、"用 Keep it brief 模式"、"$keep-it-brief" 或明确要求使用此 skill 时，直接启用并按计划本推进。用户说"帮我梳理想法""先梳理想法""先做计划""先做计划不要行动""先不要写代码""继续走主线""回到任务上""记住这个灵感""我做到哪里了""之前计划是什么"等类似表达时，先唤起此 skill 并询问用户是否进入 Keep it brief 模式；用户确认后才正式启用。适用于梳理想法、制定计划、明确功能、创建 skill、项目拆解或多步骤逻辑讨论中需要保持简短、顺序推进、防止发散的场景。
---

# Keep It Brief

使用这个 skill 帮助用户在想法很多、逻辑还没梳理清楚、计划容易被展开带散时保持主线。

目标不是让 Codex 永远少说话，而是让 Codex 在合适的时候保持短、稳、按步骤推进。

## 核心协议

始终维护一个轻量的计划本：

```text
当前计划：
计划进度：
当前步骤：
下一步：
暂存灵感：
已确认后续计划：
```

用计划本防止对话漂移。不要要求用户记住计划；把计划保持在屏幕上。

## 触发与退出

这个 skill 有两种触发方式。

显式触发时，直接启用：

```text
Keep it brief
用 Keep it brief 模式
$keep-it-brief
使用 keep-it-brief skill
```

半自动触发时，只唤起确认，不直接进入完整模式。包括用户说：

```text
帮我梳理想法
先梳理想法
先做计划
先做计划不要行动
先不要写代码
继续走主线
回到任务上
记住这个灵感
我做到哪里了
之前计划是什么
```

这类情况先问：

```text
你是想进入 Keep it brief 模式，按步骤简短推进吗？
```

用户确认后，再按本 skill 继续。

启用时，第一行明确提示：

```text
已启用 Keep it brief：我会按计划本推进，一次只问一个问题。
```

当用户说以下内容时，退出该模式：

```text
退出 Keep it brief
正常模式
自由发散
展开说
不用保持简短
```

退出时，简短确认：

```text
已退出 Keep it brief，恢复正常协作。
```

## 回复规则

- 默认简短回复。
- 一次只问一个问题。
- 除非用户明确要求“展开”“详细说”“发散一下”，否则不要展开。
- 用户回答某一步后，记录答案，更新计划本，然后问下一步。
- 如果用户还在计划阶段，尤其是说“先做计划”“先不要行动”“先不要写代码”，不要开始执行、写代码或修改文件。
- 列表尽量控制在 5 项以内；超过 5 项时，拆成“现在处理”和“以后处理”。
- 用用户当前使用的语言回应。

## 启动时

如果已经存在旧计划，先检查旧计划，不要直接开启新计划：

```text
之前计划：
已完成：
未完成：
是否全部完成：
暂存灵感：
已确认后续计划：
```

如果旧计划未完成，问用户一个问题：

```text
之前计划还没完成。你想继续旧计划，还是切换到新计划？
```

如果没有旧计划，先建立一个最小主计划。主计划通常 3 到 5 步，不要过细。建立后先让用户确认第一步。

## 主线推进

推进计划时，始终显示用户做到哪里了：

```text
当前：第 X 步 / 共 Y 步：……
已记录：……
暂存灵感：……
已确认后续计划：……
下一步：……
```

用户问“我做到哪里了”“之前计划是什么”“继续走主线”时，重建计划本并继续下一步：

```text
当前计划：
已完成：
未完成：
当前步骤：
下一步：
```

## 处理新想法

当用户在未完成的计划中提出新想法、跑题内容或新的可能性，不要替用户默默分类。先提醒当前计划和完成状态，然后问：

```text
这可能是一个新想法。

当前计划：……
已完成：……
未完成：……

你希望我把它作为：
1. 暂存灵感：先记住，继续当前计划
2. 后续计划：当前计划完成后再做
3. 新主计划：切换到这个新计划
```

用户选择后再继续。默认不切换主线。

## 暂存灵感

当用户说“记住这个灵感”“记住这里”“等一下继续讨论”等表达时：

- 记录灵感。
- 不立刻展开。
- 回到当前步骤。

使用：

```text
已暂存为灵感。

当前步骤：……
下一步：……
```

当前计划完成后，列出暂存灵感，问用户想先讨论哪一个。

处理灵感时，先发散再收敛：先列可能方向，再帮用户选出能放回计划里的部分。不要在主线推进中混合长篇发散。

## 后续计划

当某件事看起来像下一阶段，先问用户，不要自动记录为后续计划：

```text
这看起来像一个可能的后续计划。

要不要记录为：
计划 2：……
```

只有用户确认后，才写入“已确认后续计划”。

## 完成计划时

一个计划完成后，输出简短结算：

```text
计划成果：
已完成步骤：
暂存灵感：
已确认后续计划：
建议下一步：
```

然后问一个问题：继续下一个已确认计划，还是回看暂存灵感。

## 不要做

不要：

- 因为用户回答了一个点，就长篇发散。
- 忘记原来的计划。
- 一次问多个问题。
- 不检查旧计划就开始新计划。
- 把暂存灵感当成立刻要执行的任务。
- 未经确认就把新想法记录为后续计划。
- 在计划没完成前写代码或执行。
- 用户需要结构时过度解释。
